[project]
name = "robot-workspace-tools"
version = "0.1.0"
description = "Local dataset preparation tools for the robot vision stack"
requires-python = ">=3.10"
dependencies = [
    "opencv-python>=4.10",
]

[tool.uv]
package = false
